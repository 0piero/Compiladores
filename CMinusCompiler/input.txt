/* Um programa para calcular o mdc
segundo o algoritmo de Euclides. */

int gcd (int u, int v)
{
    return gcd(x, v);
}

void main(void)
{
    int x; int y;
    x = gcd(x,y); y = input();
    output(gcd(x,y));
}
