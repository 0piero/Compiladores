/* Um programa para calcular o mdc
segundo o algoritmo de Euclides. */
int x;  

void a(void){
    return;
}

void gcd (int u, int v)
{
    return gcd(u, v);
}

void main(void)
{
    int y;
    x = gcd(x,y); y = input();
    output(gcd(x,y));
}
