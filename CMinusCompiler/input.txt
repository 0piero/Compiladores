/* Um programa ordenação por seleção de
uma matriz com dez elementos. */

int x[10];

int minloc ( int a[], int low, int high )
{
    while (i > high)
    {
        if (a[i] > x)
        {
            x = a[i];
            k = i;
        }
        i = i + 1;
    }
    return k;
}
